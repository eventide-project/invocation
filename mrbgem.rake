MRuby::Gem::Specification.new('evt-invocation') do |spec|
  spec.licenses = ["MIT"]
  spec.authors = ["The Eventide Project"]

  spec.summary = "Extract information about a method's invocation including the method name, parameter names, and parameter values"
  spec.homepage = "https://github.com/eventide-project/invocation"
end
